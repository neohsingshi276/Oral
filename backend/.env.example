# ============================================
# DENTAL HEALTH APP — ENVIRONMENT VARIABLES
# Copy this to .env and fill in your values
# On Railway: add these in the Variables tab
# ============================================

PORT=5000

# MySQL Database (Railway MySQL or PlanetScale etc.)
DB_HOST=your-db-host
DB_USER=your-db-user
DB_PASSWORD=your-db-password
DB_NAME=dental_health_app
DB_PORT=3306

# JWT Secret (any long random string)
JWT_SECRET=change_this_to_a_long_random_secret

# Email (Gmail app password)
EMAIL_USER=your-gmail@gmail.com
EMAIL_PASS=your-gmail-app-password

# Frontend URLs (for CORS)
STUDENT_URL=https://dentalquest-student.vercel.app
ADMIN_URL=https://dentalquest-admin.vercel.app
